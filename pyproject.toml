[project]
name = "signalfeed"
version = "0.1.0"
description = "A minimal RSS-to-Feishu signal feed"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "lark-oapi>=1.7.2",
    "openai>=2.53.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ruff>=0.16.2",
]
